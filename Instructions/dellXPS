to do 
    terminal theme
    vim color scheme
    atom extensions
    fix folder colors



rufus, DD disk format for the .iso
gdisk for partitioning
.ext4 for root directory
follow tutorial (fstab...)

--- dualboot ---
grub bootloader
----------------

gnome desktop environment
gdm for login


First Steps:
    https://wiki.archlinux.org/index.php/Dell_XPS_15_(9550)
        fix some of the ACPI errors     
            i915.edp_vswing=2 i915.preliminary_hw_support=1 intel_idle.max_cstate=1 acpi_backlight=native acpi_osi=Linux 
        
        touchpad 
            settings -> devices -> trackpad -> tap to click 
            settings -> devices -> trackpad -> natural scrolling 
        
        shortcuts
            set hide all windows as super+d 
            cntl alt t  = gnome-terminal
        
        install
            pacman -Syu git atom unzip gcc mono perl ocaml python pip ruby vim flashplugin vlc libreoffice libinput libinput-gestures gnome-tweak-tool powertop xf86-video-intel jpegoptim
            
            eclipse 
                https://wiki.archlinux.org/index.php/eclipse
            
            matlab
                https://wiki.archlinux.org/index.php/matlab
            
            sass
                PATH="$(ruby -e 'print Gem.user_dir')/bin:$PATH"
                sudo gem install sass --no-user-install
            
            prolog????
            vmware???
            dronekit???

        graphics cards
            https://wiki.archlinux.org/index.php/Bumblebee#Installation




atom
    platformio-ide-terminal

configure git
    git config --global user.name "ThatGuyNamedTim"
    git config --global user.email "tim.j.thoresen@example.com"
    


Extensions
    Dash to dock
    Alternate-tab
    user themes

AUR install 
    Yaourt
        firefox
        chrome
        ttf-ms-fonts	
        icons: papirus icon theme
            make folders red
            red mountain background
        applications: arc theme darker
        shell: arc-dark
        terminal color 
            https://github.com/denysdovhan/one-gnome-terminal
        dronekit
        cursor: capitaine-cursors with customized move stuff

internet 
    systemctl enable NetworkManager.service

~/vimrc -----------------------------------

command W w "allow caps
command Wq wq "allow caps
command WQ wq "allow caps
command Q q "allow caps

syntax on "nice coloring of syntax
set number "adds line numbers
set expandtab "spaces for tabs 
set sw=4 "default to tabstop
set tabstop=4 "visual spaces per tab
set softtabstop=4 "tab spaces while editing
set wildmenu "visual autocomplete
set incsearch "search as characters are entered
set hlsearch "highlight matches
set backspace=indent,eol,start "allows use of backspace
set autoindent "new lines inherit the indent from the previous lines
set history=50 "improves the undo property
set background=light
set termguicolors
colorscheme PaperColor
set background=dark
---------------------------------------------

